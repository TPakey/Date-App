# Copy this file to .env and fill in keys before deploying

# Required for the places proxy (if you plan to use Google Places)
GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_API_KEY_HERE

# Required for the AI ideas generator (OpenAI)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# Notes:
# - Do NOT commit real keys to the repo. Use Vercel / deployment env settings.
# - If you run the app in mock/offline mode, no backend keys are required.
